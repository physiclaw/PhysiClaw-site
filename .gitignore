node_modules
.vercel
dist
.astro
.DS_Store
.vscode

# Installers staged from /install into public/ at build time (see scripts/stage-installers.mjs).
# The source of truth is /install, synced from the PhysiClaw repo by a GitHub Action.
public/install.sh
public/install.ps1

# Build artifacts written into public/downloads/ at build time:
#  - release assets fetched from the PhysiClaw repo (scripts/fetch-downloads.mjs)
#  - the official-skills zip + checksum + latest.json (scripts/build-official-skills.mjs)
public/downloads/
