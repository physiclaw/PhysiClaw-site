node_modules
.vercel
dist
.astro
.DS_Store
.vscode

# Installers staged from /install into public/ at build time (see scripts/stage-installers.mjs).
# The source of truth is /install, synced from the PhysiClaw repo by a GitHub Action.
public/install.sh
public/install.ps1

# Firmware fetched from the PhysiClaw GitHub release at build time (see scripts/fetch-firmware.mjs)
public/downloads/
